# plusclass-releases

`plusclass` 설치 파일만 올라오는 배포 전용 저장소입니다. 소스 코드는 들어 있지 않습니다.

프로그램을 받으실 분은 이 화면이 아니라 아래에서 받으세요.

- https://eduitit.site/downloads/plusclass/

설치 파일은 `Releases` 에 자동으로 올라오며, 태그를 밀면 GitHub Actions 가 빌드해서 업로드합니다.
